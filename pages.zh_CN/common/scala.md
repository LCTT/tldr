# scala

> Scala 应用程序启动器和可交互的解释器

- 启动一个 Scala 命令交互界面(REPL)：

`scala`

- 执行一个 Scala 脚本程序：

`scala {{script.scala}}`

- 执行一个 `.jar` 程序：

`scala {{filename.jar}}`

- 在命令行中执行单个 Scala 命令：

`scala -e {{command}}`

[#]: contributors: ([Eski]，[喀秋莎])